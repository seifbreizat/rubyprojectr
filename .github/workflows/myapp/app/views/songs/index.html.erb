<h1>Music List</h1>

<%= link_to 'New Song', new_song_path %>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to song.title, song_path(song) %> by <%= song.artist %>
      <%= link_to 'Edit', edit_song_path(song) %> |
      <%= link_to 'Delete', song_path(song), method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>
  <% end %>
</ul>
